[中文](SECURITY.zh-CN.md) · [English](SECURITY.md) · **文档：** [中文说明](README.zh-CN.md) · [用法](docs/USAGE.zh-CN.md) · [安全](docs/SAFETY.zh-CN.md) · [限制](docs/LIMITATIONS.zh-CN.md) · [行动目录](docs/CATALOG.md) · [代码规范](docs/LINTING.md)

# 安全政策

## 范围

WinCleanKit 以管理员权限修改操作系统设置。因此与本项目相关的安全属性是：

1. **不得做用户没有选择的事。** Catalog 是「什么可以被改动」的完整描述。如果存在一条能绕过 catalog 修改系统的代码路径，那就是漏洞。
2. **必须如实报告失败。** 被静默丢弃的写入、被跳过的动作，绝不能报告为成功。
3. **不得削弱机器的安全态势。** 任何动作都不得禁用 Defender、防火墙、BitLocker、SmartScreen 或 Windows Update。
4. **权限处理必须最小化。** 前端提权只为写 `HKLM`，仅此而已。

## 支持的版本

| 版本 | 支持 |
|---|---|
| `0.1.x` | ✅ |

## 报告漏洞

请**不要**为安全问题开公开 issue。请通过仓库的私密安全公告报告，或直接联系维护者。请包含：

- 受影响的版本（或 commit）
- 确切的命令或菜单路径
- 你预期发生什么、实际发生了什么
- 若已运行过，`run.log` 的相关片段
- 该问题是否需要在已被入侵的机器上才能触发

维护者看到后会尽快确认。请给修复和发布留出合理时间，再行公开披露。

## 已知且接受的限制

以下属于**已成文的行为**，不是漏洞：

- **本工具按设计会做系统级改动。** 在你不负责管理的机器上运行，或不看预览就执行，属于使用问题而非缺陷。
- **受保护的注册表键可能拒绝写入。** 引擎会如实报告。它**不会**去夺取键的所有权来强行修改 —— 强行夺取系统键所有权，比它想改的那个设置本身后果更糟。
- **已卸载的 Store 应用不会自动还原。** 还原脚本会列出需要重装的内容。
- **在专业版/家庭版上 `AllowTelemetry = 0` 会被收敛到 *必需* 级。** 平台限制，见 [LIMITATIONS.zh-CN.md](docs/LIMITATIONS.zh-CN.md)。

## 我们不会做的事

以下请求经常出现，这里明确说明会被拒绝：

- 强行夺取受保护的注册表键、服务或计划任务的所有权，或重写其 ACL。
- 通过修改 `hosts` 文件屏蔽微软域名。
- 禁用 Windows Defender、防火墙、BitLocker 或 Windows Update。
- 附带任何捆绑二进制、安装器或下载器。本项目只有脚本与数据。
